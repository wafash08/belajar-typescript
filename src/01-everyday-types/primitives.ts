export const number: number = 2;

export const string: string = "Halo";

export const isTrue: boolean = false;

let name = undefined; // sama dengan let name;
name = 0;
console.log(name);

var alamat = undefined;
var address;

typeof alamat === address;
